OS(MAC, KC_LCTL, KC_LGUI)
OS(MAC, KC_LGUI, KC_LCTL)

OS(MAC, KC_LEFT, GUI, LALT(KC_LEFT))
OS(MAC, KC_LEFT, GUI, SHIFT, LSA(KC_LEFT))

OS(MAC, KC_RIGHT, GUI, LALT(KC_RIGHT))
OS(MAC, KC_RIGHT, GUI, SHIFT, LSA(KC_RIGHT))

OS(MAC, KC_HOME, GUI, KC_PGUP)
OS(MAC, KC_END, GUI, KC_PGDN)

OS(MAC, CK_CTLZ, LGUI(KC_Z))
OS(MAC, CK_CTLX, LGUI(KC_X))
OS(MAC, CK_CTLC, LGUI(KC_C))
OS(MAC, CK_CTLV, LGUI(KC_V))

OS(WIN, CK_CTLZ, LCTL(KC_Z))
OS(WIN, CK_CTLX, LCTL(KC_X))
OS(WIN, CK_CTLC, LCTL(KC_C))
OS(WIN, CK_CTLV, LCTL(KC_V))
